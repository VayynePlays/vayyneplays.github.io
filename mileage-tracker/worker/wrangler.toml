name            = "uber-mileage-worker"
main            = "index.js"
compatibility_date = "2024-09-01"

# Secrets are NOT stored here — set them with:
#   wrangler secret put UBER_CLIENT_ID
#   wrangler secret put UBER_CLIENT_SECRET
